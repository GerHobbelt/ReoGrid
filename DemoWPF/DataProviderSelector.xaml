<UserControl x:Class="unvell.ReoGrid.WPFDemo.DataProviderSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:unvell.ReoGrid.WPFDemo"
             mc:Ignorable="d" 
             d:DesignHeight="450" Height="250" d:DesignWidth="800">
    <Grid>
        <ListBox ItemsSource="{Binding Path=ItemsSource}" SelectedValue="{Binding Path=SelectedItem}">
            
        </ListBox>
    </Grid>
</UserControl>
